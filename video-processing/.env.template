# Copy to .env (repo-root .env is also read) and `source .env` before running.
# Only GOOGLE_API_KEY is required.
GOOGLE_API_KEY=

# Optional model overrides (defaults live in src/config.py)
# VSS_VLM_MODEL=gemini-3.1-flash-lite
# VSS_LLM_MODEL=gemini-3.1-flash-lite
# VSS_EMBED_MODEL=gemini-embedding-2-preview
